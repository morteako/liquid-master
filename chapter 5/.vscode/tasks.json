{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "liquid",
            "type": "process",
            "command": "cd ${fileDirname}) && liquid ${relativeFile}"
            // "command": "ghc -e '1+1'"
        }
    ]
}